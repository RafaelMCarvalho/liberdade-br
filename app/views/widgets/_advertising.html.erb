<% if configuration.ad_published %>
  <div class="sidebar_box_wrapper" id="ad_box">
    <div class="sidebar_box">
      <p class="buttom">
        <%= link_to 'Acesse agora', configuration.ad_url,
          :target => ('_blank' if configuration.ad_new_tab) %>
      </p>
    </div>
  </div>

  <style type="text/css">
    #ad_box .sidebar_box {
      background: url("<%= configuration.ad_image.url(:normal) %>") no-repeat center;
    }
  </style>
<% end %>
